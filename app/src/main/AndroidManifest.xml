<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RestaurantApplication"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AddMenuItems" />
        <activity android:name=".DeleteMenuItems" />
        <activity android:name=".EditMenuItems" />
        <activity android:name=".StaffBookingPage" />
        <activity android:name=".StaffSettings" />
        <activity android:name=".StaffHomePage" />
        <activity android:name=".HomePage" />
        <activity android:name=".RegistrationPage" />
        <activity android:name=".GuestBookings" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MakeBookingGuest" />
        <activity android:name=".GuestSettings" />
    </application>

</manifest>
